//! Legacy components still needed by the courses pages and
//! by a few new-module generate forms. The rest moved to
//! `crate::ui`, `crate::home`, and `crate::game_engine`.

pub mod course;
pub mod game_shared;
pub mod top_bar;
